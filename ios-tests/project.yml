name: NativeEditorTests

configs:
  Debug: debug
  Release: release

options:
  minimumXcodeGenVersion: 2.38.0
  deploymentTarget:
    iOS: "16.4"
  createIntermediateGroups: true
  groupSortPosition: top
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: 5.9
    NODE_PATH: $(SRCROOT)/../example/node_modules
    IOS_DEVELOPMENT_TEAM: ""
    DEVELOPMENT_TEAM: $(IOS_DEVELOPMENT_TEAM)
    CODE_SIGN_STYLE: Automatic
    "CODE_SIGN_IDENTITY[sdk=iphoneos*]": Apple Development
    "CODE_SIGNING_ALLOWED[sdk=iphoneos*]": YES
    "CODE_SIGNING_REQUIRED[sdk=iphoneos*]": YES
    "CODE_SIGNING_ALLOWED[sdk=iphonesimulator*]": NO
    "CODE_SIGNING_REQUIRED[sdk=iphonesimulator*]": NO

targets:
  NativeEditorTestHost:
    type: application
    platform: iOS
    deploymentTarget: "16.4"
    sources:
      - path: TestHost
    settings:
      base:
        PRODUCT_NAME: NativeEditorTestHost
        PRODUCT_BUNDLE_IDENTIFIER: com.apollohg.NativeEditorTestHost
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: NativeEditorTestHost
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: NO
        INFOPLIST_KEY_UIRequiresFullScreen: YES
        LD_RUNPATH_SEARCH_PATHS:
          - $(inherited)
          - "@executable_path/Frameworks"
          - "@loader_path/Frameworks"

  NativeEditorTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "16.4"
    hostApplication: NativeEditorTestHost
    sources:
      - path: ../ios
        excludes:
          - Tests
          - EditorCore.xcframework
          - editor_coreFFI
          - editor_coreFFI.h
          - editor_coreFFI.modulemap
          - Viewer/PreparedProseInstrumentation.swift
      # Keep the debug/device collector in the actual checked-in source phase
      # exactly once; the broad ios source root excludes this explicit entry.
      - path: ../ios/Viewer/PreparedProseInstrumentation.swift
      - path: ../common/cpp/react/renderer/components/PreparedProseViewer/PreparedProseViewerShadowNode.cpp
      - path: ../ios/Tests
      # Bundled so RenderBridgeTests can read the shared security fixtures on
      # physical devices, where the repository host path is unavailable.
      - path: ../scripts/tests/security-contract-fixtures.json
        buildPhase: resources
      # Device benchmarks load the checked-in literal through Bundle APIs.
      - path: ../scripts/tests/viewer-performance-corpus.json
        buildPhase: resources
      - path: ../scripts/tests/prepared-prose-benchmark-config.json
        buildPhase: resources
      - path: ../scripts/tests/prepared-prose-harness-static-fixtures.json
        buildPhase: resources
    settings:
      base:
        PRODUCT_NAME: NativeEditorTests
        PRODUCT_BUNDLE_IDENTIFIER: com.apollohg.NativeEditorTests
        GENERATE_INFOPLIST_FILE: YES
        TEST_TARGET_NAME: NativeEditorTestHost
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/NativeEditorTestHost.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/NativeEditorTestHost
        BUNDLE_LOADER: $(TEST_HOST)
        INFOPLIST_KEY_CFBundleDisplayName: NativeEditorTests
        SWIFT_OBJC_INTERFACE_HEADER_NAME: ReactNativeProseEditor-Swift.h
        LD_RUNPATH_SEARCH_PATHS:
          - $(inherited)
          - "@loader_path/Frameworks"
          - "@executable_path/Frameworks"
        HEADER_SEARCH_PATHS:
          - $(inherited)
          - $(SRCROOT)/../ios/editor_coreFFI
          - $(SRCROOT)/../common/cpp
        OTHER_SWIFT_FLAGS:
          - $(inherited)
          - -Xcc
          - -fmodule-map-file=$(SRCROOT)/../ios/editor_coreFFI/module.modulemap
        SWIFT_INCLUDE_PATHS:
          - $(inherited)
          - $(SRCROOT)/../ios/editor_coreFFI
        "OTHER_LDFLAGS[sdk=iphoneos*]":
          - $(inherited)
          - $(PODS_CONFIGURATION_BUILD_DIR)/ReactCodegen/libReactCodegen.a
          - $(SRCROOT)/../ios/EditorCore.xcframework/ios-arm64/libeditor_core.a
        "OTHER_LDFLAGS[sdk=iphonesimulator*]":
          - $(inherited)
          - $(PODS_CONFIGURATION_BUILD_DIR)/ReactCodegen/libReactCodegen.a
          - $(SRCROOT)/../ios/EditorCore.xcframework/ios-arm64_x86_64-simulator/libeditor_core.a
    dependencies:
      - target: NativeEditorTestHost

schemes:
  NativeEditorTests:
    build:
      targets:
        NativeEditorTestHost: all
        NativeEditorTests: all
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: NativeEditorTests
  NativeEditorPreparedProsePerformance:
    build:
      targets:
        NativeEditorTestHost: all
        NativeEditorTests: all
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: NativeEditorTests
    run:
      config: Debug
      environmentVariables:
        PREPARED_PROSE_DEVICE_BENCHMARK: "1"
