import React from 'react';
import { AppRegistry, View } from 'react-native';
import {
  RichTextViewer,
  RichTextEditor,
} from '@apollohg/react-native-rich-text-editor';

function App() {
  void RichTextViewer;
  void RichTextEditor;
  return React.createElement(View);
}

AppRegistry.registerComponent('RN076Consumer', () => App);
